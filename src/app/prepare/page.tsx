import type { Metadata } from "next";
import { PrepareContent } from "@/components/localized-pages";
export const metadata: Metadata={title:"Prepare my family"};
export default function PreparePage(){return <PrepareContent/>}
